# 2026-06-11

## Sandbox Navigation And Event Summaries

- Added `GET /demo/scenarios/catalog` to parse committed scenario documents and
  suites into a compact catalog with run endpoints, formats, step counts,
  assertion counts, timeline assertion counts, and suite entry counts.
- Added `GET /demo/events/summary` to group retained sandbox events by kind,
  source, flow id, key, and tag while preserving the latency summary.
- Linked both endpoints from the dashboard, presets, config URLs, README,
  HTTP collection, OpenAPI document, and generated-client smoke fixture.
- Extended sandbox route tests so catalog and event-summary responses are
  exercised after real cache events are produced.
