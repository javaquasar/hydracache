# 2026-06-01

## Summary

Started HydraCache `0.9.0` with database query API ergonomics. The main goal is
to reduce repeated key/tag boilerplate while preserving the explicit cache
boundary as the source of truth.

## 0.9.0 Implementation Notes

- Bumped the workspace version to `0.9.0`.
- Added `DbCache::entity` and `DbCache::collection` as database query ergonomics
  over the explicit descriptor API.
- Added `DbQuery::for_entity` and `DbQuery::collection_tag` for composing
  generated entity keys/tags with manual query descriptors.
- Kept explicit `.key(...)`, `.tag(...)`, `.ttl(...)`, and `.fetch_with(...)`
  as the full-control API and future macro expansion target.
- Extended unit tests for escaped generated keys/tags, explicit key override,
  tag preservation, cache hits, and invalidation.
- Extended the Postgres testcontainers test to verify the new helpers against a
  real SQLx/Postgres flow.
- Added release notes in [0.9.0.md](../releases/0.9.0.md).

## Validation

- `cargo fmt --all -- --check`
- `cargo check --workspace --all-targets --locked`
- `cargo test --workspace --all-targets --locked`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
- `cargo test --doc --workspace --locked`
- `cargo doc --workspace --no-deps --locked`
- `cargo llvm-cov --workspace --all-targets --locked --summary-only`

Coverage summary after this change:

- functions: `100.00%`
- total lines: `99.72%`
- Postgres testcontainers integration test: passed with Docker available

## 0.10.0 Implementation Notes

- Bumped the workspace version to `0.10.0`.
- Added `CacheEntity` as a manual domain metadata trait in `hydracache-db`.
- Re-exported `CacheEntity` from `hydracache-sqlx`.
- Added `DbCache::for_entity<T: CacheEntity>(id)` for short domain-shaped query
  descriptors.
- Added `DbQuery::for_cache_entity(id)` so existing descriptor chains can apply
  entity metadata while preserving existing tags.
- Automatically adds entity key, entity tag, and optional collection tag from
  `CacheEntity` metadata.
- Extended unit tests for metadata generation, escaping, optional collection
  tags, tag preservation, cache hits, and collection-tag invalidation.
- Extended the Postgres testcontainers test to verify `CacheEntity` descriptors
  against a real SQLx/Postgres flow.
- Added release notes in [0.10.0.md](../releases/0.10.0.md).
