schema_version = 1
id = "resp-open-loop-connection-pipeline-v1"
seed = 670304
offered_rate_per_second = 2000
operations_per_repeat = 200000
repeats = 5
preload_entries = 10000
key_count = 10000
payload_bytes = 256
batch_size = 10
connections = [1, 10, 100]
pipelines = [1, 10]
metric = "scheduled_send_p99_microseconds"
methodology = "open-loop"
state_scope = "node-local"
selected_endpoint_only = true
repeat_isolation = "logical-keyspace-reset-and-counter-zero"
daemon_reused_across_repeats = true
# Five 200k-exchange repeats on the admitted AX42 host established a 16.5%-25.9%
# scheduled-send p99 envelope across retained campaigns. Keep a reviewable 30%
# characterization bound; capacity knees and SLO/error gates remain stricter.
robust_spread_tolerance = 0.30
