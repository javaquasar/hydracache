schema_version = 1
release = "0.71"
discovery_scope = "publishable-workspace-production-rust-v1"

[[owner]]
owner_id = "owner-000b9493c0933d6c"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/elasticity.rs"
symbol = "ZoneAwareReplicationStrategy"
allocation_sites = ["topology"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ZoneAwareReplicationStrategy"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-000b9493c0933d6c.count", "owner-000b9493c0933d6c.bytes", "owner-000b9493c0933d6c.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-013895b1f50ccce6"
subsystem = "hydracache"
owning_type = "Arc + Vec"
source = "crates/hydracache/src/cluster/runtime.rs"
symbol = "ClusterRuntime"
allocation_sites = ["control_plane", "discovery", "bootstrap", "lifecycle"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterRuntime"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-013895b1f50ccce6.count", "owner-013895b1f50ccce6.bytes", "owner-013895b1f50ccce6.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-022f68fdfc27e715"
subsystem = "hydracache-transport-redis"
owning_type = "Arc + Client + Receiver"
source = "crates/hydracache-transport-redis/src/lib.rs"
symbol = "RedisInvalidationTransport"
allocation_sites = ["config", "inbound"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RedisInvalidationTransport"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "unavailable(external wrapper owns internal count)"
byte_bound = "unavailable(external provider owns byte accounting)"
age_bound = "unavailable(external provider controls lifetime)"
overflow_behavior = "provider error propagates; wrapper does not retry silently"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "external_allocator"
disposition_reason = "HydraCache owns the wrapper boundary; provider internals remain explicitly uncertain (external_type)."

[[owner]]
owner_id = "owner-0529df981378913c"
subsystem = "hydracache-cluster"
owning_type = "Arc + Vec"
source = "crates/hydracache-cluster/src/lib.rs"
symbol = "HydraCluster"
allocation_sites = ["bootstrap", "discovery", "control_plane"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraCluster"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-0529df981378913c.count", "owner-0529df981378913c.bytes", "owner-0529df981378913c.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-07e1e47a65e235b4"
subsystem = "hydracache-cluster-transport-axum"
owning_type = "Arc + BTreeMap + Bytes"
source = "crates/hydracache-cluster-transport-axum/src/lib.rs"
symbol = "MemoryPeerFetchStore"
allocation_sites = ["values"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate MemoryPeerFetchStore"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-07e1e47a65e235b4.count", "owner-07e1e47a65e235b4.bytes", "owner-07e1e47a65e235b4.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-080356fd4d8a973f"
subsystem = "hydracache-client-transport-axum"
owning_type = "Receiver"
source = "crates/hydracache-client-transport-axum/src/lib.rs"
symbol = "ClientSurfaceMutationSubscriber"
allocation_sites = ["receiver"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClientSurfaceMutationSubscriber"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-082b91553443ae35"
subsystem = "hydracache-client-hc2"
owning_type = "Arc"
source = "crates/hydracache-client-hc2/src/client.rs"
symbol = "Hc2Client"
allocation_sites = ["handle", "cluster_id"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate Hc2Client"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-082b91553443ae35.count", "owner-082b91553443ae35.bytes", "owner-082b91553443ae35.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-085dddb95e712413"
subsystem = "hydracache"
owning_type = "Arc + Vec"
source = "crates/hydracache/src/cluster/runtime.rs"
symbol = "HydraCacheMemberBuilder"
allocation_sites = ["bootstrap", "control_plane", "discovery"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraCacheMemberBuilder"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-0f7358ddae71272d"
subsystem = "hydracache-cluster"
owning_type = "Vec"
source = "crates/hydracache-cluster/src/lib.rs"
symbol = "HydraClusterBuilder"
allocation_sites = ["bootstrap", "chitchat_seeds"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraClusterBuilder"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-13938415e3ca0f25"
subsystem = "hydracache"
owning_type = "Arc + Receiver"
source = "crates/hydracache/src/events.rs"
symbol = "CacheEventSubscriber"
allocation_sites = ["receiver", "stats"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheEventSubscriber"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-13ae78ff95fb3617"
subsystem = "hydracache-db"
owning_type = "Arc"
source = "crates/hydracache-db/src/outbox.rs"
symbol = "InvalidationOutboxWorker"
allocation_sites = ["counters"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InvalidationOutboxWorker"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-13ae78ff95fb3617.count", "owner-13ae78ff95fb3617.bytes", "owner-13ae78ff95fb3617.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-14d3abe0d151fa47"
subsystem = "hydracache-client-transport-axum"
owning_type = "Arc"
source = "crates/hydracache-client-transport-axum/src/lib.rs"
symbol = "AxumClientSurface"
allocation_sites = ["state"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate AxumClientSurface"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-14d3abe0d151fa47.count", "owner-14d3abe0d151fa47.bytes", "owner-14d3abe0d151fa47.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-166b4725aa0021be"
subsystem = "hydracache-cluster-raft"
owning_type = "Vec"
source = "crates/hydracache-cluster-raft/src/log_store.rs"
symbol = "InMemoryRaftLogState"
allocation_sites = ["entries"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryRaftLogState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-166b4725aa0021be.count", "owner-166b4725aa0021be.bytes", "owner-166b4725aa0021be.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-179c497a43bde431"
subsystem = "hydracache"
owning_type = "Bytes + Vec"
source = "crates/hydracache/src/entry.rs"
symbol = "CacheEntry"
allocation_sites = ["value", "tags"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheEntry"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-179c497a43bde431.count", "owner-179c497a43bde431.bytes", "owner-179c497a43bde431.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-19b1293c0c5800a9"
subsystem = "hydracache"
owning_type = "BTreeMap + Vec"
source = "crates/hydracache/src/grid/active_active.rs"
symbol = "ActiveActiveState"
allocation_sites = ["peers", "records", "writes", "pending"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ActiveActiveState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-19b1293c0c5800a9.count", "owner-19b1293c0c5800a9.bytes", "owner-19b1293c0c5800a9.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-1e11c1d87bcede3d"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/elasticity.rs"
symbol = "BatchInvalidationState"
allocation_sites = ["applied"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate BatchInvalidationState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-1e11c1d87bcede3d.count", "owner-1e11c1d87bcede3d.bytes", "owner-1e11c1d87bcede3d.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-1eac54c6b0294a24"
subsystem = "hydracache-client-transport-axum"
owning_type = "Vec"
source = "crates/hydracache-client-transport-axum/src/lib.rs"
symbol = "StoredValue"
allocation_sites = ["value"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate StoredValue"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-218785c2794d8fdc"
subsystem = "hydracache"
owning_type = "SealedBytes"
source = "crates/hydracache/src/grid/hardening.rs"
symbol = "ReplicatedValueRecord"
allocation_sites = ["state"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ReplicatedValueRecord"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-234bea6f7d5a9f05"
subsystem = "hydracache-transport-nats"
owning_type = "Arc + Client + Receiver"
source = "crates/hydracache-transport-nats/src/lib.rs"
symbol = "NatsInvalidationTransport"
allocation_sites = ["config", "inbound"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate NatsInvalidationTransport"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "unavailable(external wrapper owns internal count)"
byte_bound = "unavailable(external provider owns byte accounting)"
age_bound = "unavailable(external provider controls lifetime)"
overflow_behavior = "provider error propagates; wrapper does not retry silently"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "external_allocator"
disposition_reason = "HydraCache owns the wrapper boundary; provider internals remain explicitly uncertain (external_type)."

[[owner]]
owner_id = "owner-253cc1eceb17aa4b"
subsystem = "hydracache-client-hc2"
owning_type = "Weak"
source = "crates/hydracache-client-hc2/src/client.rs"
symbol = "PendingGuard"
allocation_sites = ["runtime"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate PendingGuard"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-25e2f49c24999caa"
subsystem = "hydracache-server"
owning_type = "Arc + BTreeSet"
source = "crates/hydracache-server/src/grid_host.rs"
symbol = "TestRaftOutboundFaultController"
allocation_sites = ["last_document", "logged_hits"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate TestRaftOutboundFaultController"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-25e2f49c24999caa.count", "owner-25e2f49c24999caa.bytes", "owner-25e2f49c24999caa.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-263f70a1cc5b27ea"
subsystem = "hydracache-client-transport-axum"
owning_type = "Arc + BTreeMap + Sender"
source = "crates/hydracache-client-transport-axum/src/lib.rs"
symbol = "ClientSurfaceState"
allocation_sites = ["mutation_events", "store", "idempotency_keys", "audit_sink", "audit"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClientSurfaceState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-263f70a1cc5b27ea.count", "owner-263f70a1cc5b27ea.bytes", "owner-263f70a1cc5b27ea.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-2b3e3ede457333e8"
subsystem = "hydracache-cluster-raft"
owning_type = "Arc"
source = "crates/hydracache-cluster-raft/src/lib.rs"
symbol = "InMemoryRaftMetadataStore"
allocation_sites = ["snapshot"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryRaftMetadataStore"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-2b3e3ede457333e8.count", "owner-2b3e3ede457333e8.bytes", "owner-2b3e3ede457333e8.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-2fe223c3e3de9a93"
subsystem = "hydracache-cluster-transport-axum"
owning_type = "BTreeMap"
source = "crates/hydracache-cluster-transport-axum/src/lib.rs"
symbol = "HotRemoteCacheState"
allocation_sites = ["tracked"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HotRemoteCacheState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-2fe223c3e3de9a93.count", "owner-2fe223c3e3de9a93.bytes", "owner-2fe223c3e3de9a93.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-31385fd63d5ffcbd"
subsystem = "hydracache"
owning_type = "BTreeMap + Vec"
source = "crates/hydracache/src/cluster/control_plane.rs"
symbol = "InMemoryClusterState"
allocation_sites = ["members", "clients", "events"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryClusterState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-31385fd63d5ffcbd.count", "owner-31385fd63d5ffcbd.bytes", "owner-31385fd63d5ffcbd.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-34c2f0fd0dd15cbb"
subsystem = "hydracache-server"
owning_type = "Arc"
source = "crates/hydracache-server/src/cluster_status.rs"
symbol = "LiveClusterStatus"
allocation_sites = ["grid"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate LiveClusterStatus"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-35b8cdd284f1386f"
subsystem = "hydracache-core"
owning_type = "Vec"
source = "crates/hydracache-core/src/events.rs"
symbol = "CacheEventOptions"
allocation_sites = ["include_kinds", "exclude_kinds"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheEventOptions"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-38b53454f9246dac"
subsystem = "hydracache-cluster-transport-axum"
owning_type = "Arc"
source = "crates/hydracache-cluster-transport-axum/src/lib.rs"
symbol = "PeerFetchState"
allocation_sites = ["store"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate PeerFetchState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-38b53454f9246dac.count", "owner-38b53454f9246dac.bytes", "owner-38b53454f9246dac.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-38d323909092ea71"
subsystem = "hydracache-server"
owning_type = "Arc + SharedRaftPeers"
source = "crates/hydracache-server/src/grid_host.rs"
symbol = "RaftClusterMessageHandler"
allocation_sites = ["raft", "message_sink", "raft_peers", "drive_diagnostics"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftClusterMessageHandler"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-38d323909092ea71.count", "owner-38d323909092ea71.bytes", "owner-38d323909092ea71.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-39e90e42d5e49679"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/grid/mod.rs"
symbol = "EffectiveReplicationMap"
allocation_sites = ["reading"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate EffectiveReplicationMap"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-39e90e42d5e49679.count", "owner-39e90e42d5e49679.bytes", "owner-39e90e42d5e49679.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-3a54fbfaf978fd93"
subsystem = "hydracache-client-hc2"
owning_type = "Arc + BTreeMap + CurrentConnection + Vec + Weak"
source = "crates/hydracache-client-hc2/src/reconnect.rs"
symbol = "RecoveringRuntime"
allocation_sites = ["endpoints", "subscriptions", "sessions", "metrics"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RecoveringRuntime"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "unavailable(external wrapper owns internal count)"
byte_bound = "unavailable(external provider owns byte accounting)"
age_bound = "unavailable(external provider controls lifetime)"
overflow_behavior = "provider error propagates; wrapper does not retry silently"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "external_allocator"
disposition_reason = "HydraCache owns the wrapper boundary; provider internals remain explicitly uncertain (external_type)."

[[owner]]
owner_id = "owner-3c1343755d76c77d"
subsystem = "hydracache-cluster-raft"
owning_type = "Arc + Vec"
source = "crates/hydracache-cluster-raft/src/lib.rs"
symbol = "InMemoryRaftMessageSink"
allocation_sites = ["messages"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryRaftMessageSink"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-3c1343755d76c77d.count", "owner-3c1343755d76c77d.bytes", "owner-3c1343755d76c77d.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-3ca851f6b3db53c1"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/cluster/membership.rs"
symbol = "ClusterMember"
allocation_sites = ["metadata"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterMember"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-3ca851f6b3db53c1.count", "owner-3ca851f6b3db53c1.bytes", "owner-3ca851f6b3db53c1.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-3df18e069c261cd6"
subsystem = "hydracache"
owning_type = "Sender"
source = "crates/hydracache/src/cluster/membership.rs"
symbol = "ClusterMembershipEventBus"
allocation_sites = ["sender"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterMembershipEventBus"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-3e60f515833459ee"
subsystem = "hydracache-server"
owning_type = "Arc + SharedRaftPeers"
source = "crates/hydracache-server/src/grid_host.rs"
symbol = "ClusterTransportHandles"
allocation_sites = ["raft", "message_sink", "raft_peers", "drive_diagnostics"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterTransportHandles"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-3e60f515833459ee.count", "owner-3e60f515833459ee.bytes", "owner-3e60f515833459ee.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-4083b335b291b826"
subsystem = "hydracache-cluster-raft"
owning_type = "BTreeMap + BTreeSet + Vec"
source = "crates/hydracache-cluster-raft/src/log_store.rs"
symbol = "DurableControlPlaneCluster"
allocation_sites = ["members", "reachable", "committed"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate DurableControlPlaneCluster"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-4083b335b291b826.count", "owner-4083b335b291b826.bytes", "owner-4083b335b291b826.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-40a547f5552ad0de"
subsystem = "hydracache-client-hc2"
owning_type = "Bytes"
source = "crates/hydracache-client-hc2/src/types.rs"
symbol = "CacheValue"
allocation_sites = ["value"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheValue"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-40cd1f18d4fd1faf"
subsystem = "hydracache-redis-compat"
owning_type = "Arc + BTreeMap"
source = "crates/hydracache-redis-compat/src/lib.rs"
symbol = "RedisRespServer"
allocation_sites = ["state", "script_cache"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RedisRespServer"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-40cd1f18d4fd1faf.count", "owner-40cd1f18d4fd1faf.bytes", "owner-40cd1f18d4fd1faf.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-411490359771e3b7"
subsystem = "hydracache-client-hc2"
owning_type = "Weak"
source = "crates/hydracache-client-hc2/src/reconnect.rs"
symbol = "RecoveringSessionState"
allocation_sites = ["runtime"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RecoveringSessionState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-411490359771e3b7.count", "owner-411490359771e3b7.bytes", "owner-411490359771e3b7.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-43b94931bba59702"
subsystem = "hydracache-core"
owning_type = "Vec"
source = "crates/hydracache-core/src/options.rs"
symbol = "CacheOptions"
allocation_sites = ["tags"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheOptions"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-45d8248fa2db52b5"
subsystem = "hydracache-cluster-raft"
owning_type = "Arc"
source = "crates/hydracache-cluster-raft/src/log_store.rs"
symbol = "RaftStorageFaultController"
allocation_sites = ["inner"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftStorageFaultController"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-45d8248fa2db52b5.count", "owner-45d8248fa2db52b5.bytes", "owner-45d8248fa2db52b5.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-47dfc112a90854d4"
subsystem = "hydracache"
owning_type = "BTreeMap + BTreeSet + VecDeque"
source = "crates/hydracache/src/grid/hinted_handoff.rs"
symbol = "InMemoryHintStore"
allocation_sites = ["hints", "repair_marks"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryHintStore"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-47dfc112a90854d4.count", "owner-47dfc112a90854d4.bytes", "owner-47dfc112a90854d4.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-49fd940daac48658"
subsystem = "hydracache-cluster-raft"
owning_type = "Arc"
source = "crates/hydracache-cluster-raft/src/log_store.rs"
symbol = "DurableRaftLogStore"
allocation_sites = ["fsync_count"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate DurableRaftLogStore"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "file-format and configured retention bound"
byte_bound = "configured durable retention and filesystem capacity"
age_bound = "configured checkpoint/log retention lifecycle"
overflow_behavior = "fail-loud before exceeding durable retention or storage limits"
snapshot_fields = ["owner-49fd940daac48658.file_bytes", "owner-49fd940daac48658.records"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "file_backed"
disposition_reason = "Retention is durable/file-backed and is accounted separately from anonymous live heap."

[[owner]]
owner_id = "owner-4a2f86610d4f5638"
subsystem = "hydracache-cluster-raft"
owning_type = "Vec"
source = "crates/hydracache-cluster-raft/src/lib.rs"
symbol = "RaftMetadataSnapshotPayload"
allocation_sites = ["commands"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftMetadataSnapshotPayload"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-4c22244618e8c948"
subsystem = "hydracache-client"
owning_type = "Arc"
source = "crates/hydracache-client/src/lib.rs"
symbol = "HydraClient"
allocation_sites = ["metrics"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraClient"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-4c22244618e8c948.count", "owner-4c22244618e8c948.bytes", "owner-4c22244618e8c948.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-4c66c374f1166c86"
subsystem = "hydracache"
owning_type = "HashMap"
source = "crates/hydracache/src/inflight.rs"
symbol = "InFlightMap"
allocation_sites = ["loads"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InFlightMap"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-4c66c374f1166c86.count", "owner-4c66c374f1166c86.bytes", "owner-4c66c374f1166c86.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-4cf552ab7b7b2cc4"
subsystem = "hydracache-client"
owning_type = "Client"
source = "crates/hydracache-client/src/lib.rs"
symbol = "HttpClientTransport"
allocation_sites = []
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HttpClientTransport"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "unavailable(external wrapper owns internal count)"
byte_bound = "unavailable(external provider owns byte accounting)"
age_bound = "unavailable(external provider controls lifetime)"
overflow_behavior = "provider error propagates; wrapper does not retry silently"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "external_allocator"
disposition_reason = "HydraCache owns the wrapper boundary; provider internals remain explicitly uncertain (external_type)."

[[owner]]
owner_id = "owner-4f6e5c897815c574"
subsystem = "hydracache"
owning_type = "Bytes"
source = "crates/hydracache/src/cluster/peer_fetch.rs"
symbol = "ClusterPeerFetchResponse"
allocation_sites = ["value"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterPeerFetchResponse"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-4fee5aff3c3634b2"
subsystem = "hydracache-client-hc2"
owning_type = "Vec"
source = "crates/hydracache-client-hc2/src/types.rs"
symbol = "TopologySnapshot"
allocation_sites = ["nodes"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate TopologySnapshot"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-5616ea33420a4227"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/elasticity.rs"
symbol = "ReplicaScorer"
allocation_sites = ["observations"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ReplicaScorer"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-574b9a08cbac1526"
subsystem = "hydracache-cluster-raft"
owning_type = "BTreeSet + Vec"
source = "crates/hydracache-cluster-raft/src/lib.rs"
symbol = "StagedMetadataSnapshot"
allocation_sites = ["commands", "applied_command_ids"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate StagedMetadataSnapshot"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-59bf82f93684f9fe"
subsystem = "hydracache-actuator-axum"
owning_type = "Vec"
source = "crates/hydracache-actuator-axum/src/lib.rs"
symbol = "ClusterStagingHealthResponse"
allocation_sites = ["caches"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterStagingHealthResponse"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-5a7a99cb0a4dd29a"
subsystem = "hydracache"
owning_type = "Arc"
source = "crates/hydracache/src/cluster/admission.rs"
symbol = "ClusterAdmissionBridge"
allocation_sites = ["inner"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterAdmissionBridge"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-5a7a99cb0a4dd29a.count", "owner-5a7a99cb0a4dd29a.bytes", "owner-5a7a99cb0a4dd29a.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-5d2069b4ed71164d"
subsystem = "hydracache-cluster-raft"
owning_type = "Arc"
source = "crates/hydracache-cluster-raft/src/log_store.rs"
symbol = "InMemoryRaftLogStore"
allocation_sites = ["state"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryRaftLogStore"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-5d2069b4ed71164d.count", "owner-5d2069b4ed71164d.bytes", "owner-5d2069b4ed71164d.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-621107c3dc68d7da"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/cluster/node.rs"
symbol = "ClusterNodeConfig"
allocation_sites = ["peers"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterNodeConfig"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-62c4720f03f98f80"
subsystem = "hydracache-client-hc2"
owning_type = "Arc"
source = "crates/hydracache-client-hc2/src/client.rs"
symbol = "ClientHandle"
allocation_sites = ["runtime"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClientHandle"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-62c4720f03f98f80.count", "owner-62c4720f03f98f80.bytes", "owner-62c4720f03f98f80.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-635cc712fc835494"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/session_context.rs"
symbol = "SessionWatermark"
allocation_sites = ["seen"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate SessionWatermark"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-635cc712fc835494.count", "owner-635cc712fc835494.bytes", "owner-635cc712fc835494.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-64b279971fe0217d"
subsystem = "hydracache"
owning_type = "BTreeMap + Bytes"
source = "crates/hydracache/src/cluster/peer_fetch.rs"
symbol = "InMemoryPeerFetchState"
allocation_sites = ["values"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryPeerFetchState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-64b279971fe0217d.count", "owner-64b279971fe0217d.bytes", "owner-64b279971fe0217d.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-65c844bdfae63374"
subsystem = "hydracache-client-hc2"
owning_type = "Arc + BTreeMap + Bytes + HandshakeReply + Semaphore + Sender"
source = "crates/hydracache-client-hc2/src/client.rs"
symbol = "Runtime"
allocation_sites = ["outbound", "control", "handshake", "invocations", "pending_subscriptions", "subscriptions", "pending_sessions", "sessions", "invocation_permits", "subscription_permits", "session_permits"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate Runtime"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-65c844bdfae63374.count", "owner-65c844bdfae63374.bytes", "owner-65c844bdfae63374.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-679746923848da02"
subsystem = "hydracache-redis-compat"
owning_type = "BTreeSet + Vec"
source = "crates/hydracache-redis-compat/src/lib.rs"
symbol = "RedisConnectionState"
allocation_sites = ["channels", "patterns"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RedisConnectionState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-679746923848da02.count", "owner-679746923848da02.bytes", "owner-679746923848da02.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-6b778388024e64ed"
subsystem = "hydracache-client-protocol"
owning_type = "Vec"
source = "crates/hydracache-client-protocol/src/java_migration.rs"
symbol = "JavaClientRuntimeConfig"
allocation_sites = ["endpoints"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate JavaClientRuntimeConfig"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-6c7fe54883299b05"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/grid/mod.rs"
symbol = "Replicas"
allocation_sites = ["backups"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate Replicas"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-6c7fe54883299b05.count", "owner-6c7fe54883299b05.bytes", "owner-6c7fe54883299b05.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-6dda36b7cfb61151"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/elasticity.rs"
symbol = "ControlPlaneSnapshot"
allocation_sites = ["topology", "ownership", "tombstone_versions"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ControlPlaneSnapshot"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-6e1a67e771b4eb4d"
subsystem = "hydracache-server"
owning_type = "Client + SharedRaftPeers"
source = "crates/hydracache-server/src/grid_host.rs"
symbol = "HttpRaftMessageSink"
allocation_sites = ["peers"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HttpRaftMessageSink"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "unavailable(external wrapper owns internal count)"
byte_bound = "unavailable(external provider owns byte accounting)"
age_bound = "unavailable(external provider controls lifetime)"
overflow_behavior = "provider error propagates; wrapper does not retry silently"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "external_allocator"
disposition_reason = "HydraCache owns the wrapper boundary; provider internals remain explicitly uncertain (external_type)."

[[owner]]
owner_id = "owner-6fdfe94a74706b5d"
subsystem = "hydracache-server"
owning_type = "Arc"
source = "crates/hydracache-server/src/hc2.rs"
symbol = "Hc2ClientPlaneService"
allocation_sites = ["state", "cluster_id", "accounting"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate Hc2ClientPlaneService"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-6fdfe94a74706b5d.count", "owner-6fdfe94a74706b5d.bytes", "owner-6fdfe94a74706b5d.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7046f6765b7216e1"
subsystem = "hydracache-cluster-transport-axum"
owning_type = "Arc + Vec"
source = "crates/hydracache-cluster-transport-axum/src/lib.rs"
symbol = "MemoryClusterMessageHandler"
allocation_sites = ["messages"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate MemoryClusterMessageHandler"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-7046f6765b7216e1.count", "owner-7046f6765b7216e1.bytes", "owner-7046f6765b7216e1.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-709d1f8702f8c192"
subsystem = "hydracache-client-hc2"
owning_type = "Bytes"
source = "crates/hydracache-client-hc2/src/types.rs"
symbol = "CacheEvent"
allocation_sites = ["key", "value"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheEvent"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-726d60e7b5f10900"
subsystem = "hydracache"
owning_type = "BTreeMap + Vec"
source = "crates/hydracache/src/cluster/node.rs"
symbol = "InMemoryClusterStorage"
allocation_sites = ["values"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryClusterStorage"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-726d60e7b5f10900.count", "owner-726d60e7b5f10900.bytes", "owner-726d60e7b5f10900.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-73cb042586cadea3"
subsystem = "hydracache-client-hc2"
owning_type = "Arc"
source = "crates/hydracache-client-hc2/src/reconnect.rs"
symbol = "CurrentConnection"
allocation_sites = ["client"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CurrentConnection"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-73cb042586cadea3.count", "owner-73cb042586cadea3.bytes", "owner-73cb042586cadea3.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7516dfcbd2ea769b"
subsystem = "hydracache"
owning_type = "BTreeMap + BTreeSet"
source = "crates/hydracache/src/grid/mod.rs"
symbol = "HotCacheDirectory"
allocation_sites = ["holders"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HotCacheDirectory"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-7516dfcbd2ea769b.count", "owner-7516dfcbd2ea769b.bytes", "owner-7516dfcbd2ea769b.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7549baa7b7ae4050"
subsystem = "hydracache"
owning_type = "Receiver"
source = "crates/hydracache/src/cluster/membership.rs"
symbol = "ClusterMembershipSubscriber"
allocation_sites = ["receiver"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterMembershipSubscriber"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-75c6e70bbd8a26da"
subsystem = "hydracache-server"
owning_type = "Arc + BTreeMap + Vec"
source = "crates/hydracache-server/src/grid_host.rs"
symbol = "RaftTopology"
allocation_sites = ["voters", "peers"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftTopology"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-75c6e70bbd8a26da.count", "owner-75c6e70bbd8a26da.bytes", "owner-75c6e70bbd8a26da.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7697f61662b2e1d7"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/cluster/diagnostics.rs"
symbol = "ClusterDiagnostics"
allocation_sites = ["bootstrap"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterDiagnostics"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-7697f61662b2e1d7.count", "owner-7697f61662b2e1d7.bytes", "owner-7697f61662b2e1d7.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7977dd8759d29eda"
subsystem = "hydracache"
owning_type = "BTreeMap + VecDeque"
source = "crates/hydracache/src/grid/elasticity.rs"
symbol = "TieredValueStore"
allocation_sites = ["hot", "order"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate TieredValueStore"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-7ab9fbd68a30a164"
subsystem = "hydracache-client-hc2"
owning_type = "Bytes + Sender"
source = "crates/hydracache-client-hc2/src/client.rs"
symbol = "PendingSession"
allocation_sites = ["reply"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate PendingSession"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-7ab9fbd68a30a164.count", "owner-7ab9fbd68a30a164.bytes", "owner-7ab9fbd68a30a164.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7afded22791c74c8"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/conditional.rs"
symbol = "SingleKeyConditionalStore"
allocation_sites = ["records", "tombstone_gc_watermarks", "locks"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate SingleKeyConditionalStore"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-7afded22791c74c8.count", "owner-7afded22791c74c8.bytes", "owner-7afded22791c74c8.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7c0450ce617a58c4"
subsystem = "hydracache"
owning_type = "Arc"
source = "crates/hydracache/src/cluster/lifecycle.rs"
symbol = "ClusterLifecycleComponent"
allocation_sites = ["state"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterLifecycleComponent"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-7c0450ce617a58c4.count", "owner-7c0450ce617a58c4.bytes", "owner-7c0450ce617a58c4.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7c170b42539c74d4"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/cluster/diagnostics.rs"
symbol = "ClusterPilotReport"
allocation_sites = ["highlights"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterPilotReport"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-7c236aa8f792ed9a"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/hardening.rs"
symbol = "ClusterMergeOutcome"
allocation_sites = ["records"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterMergeOutcome"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-7d2e2df46821c91c"
subsystem = "hydracache-cluster-raft"
owning_type = "Vec"
source = "crates/hydracache-cluster-raft/src/lib.rs"
symbol = "RaftWireMessage"
allocation_sites = ["payload"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftWireMessage"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-7d2e2df46821c91c.count", "owner-7d2e2df46821c91c.bytes", "owner-7d2e2df46821c91c.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7e3302e2ee83947f"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/grid/mod.rs"
symbol = "TombstoneTracker"
allocation_sites = ["records"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate TombstoneTracker"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-7e3302e2ee83947f.count", "owner-7e3302e2ee83947f.bytes", "owner-7e3302e2ee83947f.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-7e55ecfa99330487"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/backup/pitr.rs"
symbol = "PitrLog"
allocation_sites = ["records"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate PitrLog"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "file-format and configured retention bound"
byte_bound = "configured durable retention and filesystem capacity"
age_bound = "configured checkpoint/log retention lifecycle"
overflow_behavior = "fail-loud before exceeding durable retention or storage limits"
snapshot_fields = ["owner-7e55ecfa99330487.file_bytes", "owner-7e55ecfa99330487.records"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "file_backed"
disposition_reason = "Retention is durable/file-backed and is accounted separately from anonymous live heap."

[[owner]]
owner_id = "owner-7eefe72343389df3"
subsystem = "hydracache"
owning_type = "BTreeMap + Vec"
source = "crates/hydracache/src/backup/object_store.rs"
symbol = "InMemoryObjectStore"
allocation_sites = ["objects"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryObjectStore"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-7eefe72343389df3.count", "owner-7eefe72343389df3.bytes", "owner-7eefe72343389df3.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-80b52eb0042eff0c"
subsystem = "hydracache"
owning_type = "Arc + Receiver + Sender"
source = "crates/hydracache/src/invalidation_transport.rs"
symbol = "InMemoryTransport"
allocation_sites = ["sender", "receiver"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryTransport"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-80b52eb0042eff0c.count", "owner-80b52eb0042eff0c.bytes", "owner-80b52eb0042eff0c.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-810493659a154eca"
subsystem = "hydracache"
owning_type = "Arc + Vec"
source = "crates/hydracache/src/cluster/runtime.rs"
symbol = "HydraCacheClientBuilder"
allocation_sites = ["bootstrap", "control_plane", "discovery"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraCacheClientBuilder"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-814b2f01f5622044"
subsystem = "hydracache-server"
owning_type = "Arc"
source = "crates/hydracache-server/src/hc2.rs"
symbol = "ConnectionGuard"
allocation_sites = ["accounting"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ConnectionGuard"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-83bf5191eb18e9de"
subsystem = "hydracache-macros"
owning_type = "Vec"
source = "crates/hydracache-macros/src/cacheable.rs"
symbol = "CacheableConfig"
allocation_sites = ["repeated_tags"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheableConfig"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-85bde59bce494e82"
subsystem = "hydracache"
owning_type = "Arc + Sender"
source = "crates/hydracache/src/cache.rs"
symbol = "HydraCacheInner"
allocation_sites = ["in_flight", "stats", "invalidation_bus", "bus_shutdown", "tag_index", "memory"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraCacheInner"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-85bde59bce494e82.count", "owner-85bde59bce494e82.bytes", "owner-85bde59bce494e82.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-87945c01457eba3d"
subsystem = "hydracache-cluster-raft"
owning_type = "Arc + BTreeSet + Vec"
source = "crates/hydracache-cluster-raft/src/lib.rs"
symbol = "RaftRuntimeState"
allocation_sites = ["cluster", "commands", "applied_command_ids", "results", "outbound_messages"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftRuntimeState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-87945c01457eba3d.count", "owner-87945c01457eba3d.bytes", "owner-87945c01457eba3d.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-888d55389d4ad0d2"
subsystem = "hydracache-client"
owning_type = "Arc"
source = "crates/hydracache-client/src/lib.rs"
symbol = "RemoteNearCache"
allocation_sites = ["metrics"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RemoteNearCache"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-888d55389d4ad0d2.count", "owner-888d55389d4ad0d2.bytes", "owner-888d55389d4ad0d2.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-88d9a27ddd5f9d95"
subsystem = "hydracache-cluster-raft"
owning_type = "Vec"
source = "crates/hydracache-cluster-raft/src/lib.rs"
symbol = "RaftMetadataRuntimeExport"
allocation_sites = ["commands"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftMetadataRuntimeExport"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-88d9a27ddd5f9d95.count", "owner-88d9a27ddd5f9d95.bytes", "owner-88d9a27ddd5f9d95.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-89d664d36a05c96e"
subsystem = "hydracache-client-hc2"
owning_type = "Arc + Receiver + Sender"
source = "crates/hydracache-client-hc2/src/adapter.rs"
symbol = "AdapterConnection"
allocation_sites = ["outbound", "inbound", "control"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate AdapterConnection"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-89d664d36a05c96e.count", "owner-89d664d36a05c96e.bytes", "owner-89d664d36a05c96e.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-8c05f2eccc72257f"
subsystem = "hydracache-cluster-transport-axum"
owning_type = "Arc"
source = "crates/hydracache-cluster-transport-axum/src/lib.rs"
symbol = "ClusterMessageRouteState"
allocation_sites = ["handler"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterMessageRouteState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-8c05f2eccc72257f.count", "owner-8c05f2eccc72257f.bytes", "owner-8c05f2eccc72257f.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-90a195c7d16c26a7"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/elasticity.rs"
symbol = "ZoneAwareReplicaSet"
allocation_sites = ["topology"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ZoneAwareReplicaSet"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-90a195c7d16c26a7.count", "owner-90a195c7d16c26a7.bytes", "owner-90a195c7d16c26a7.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-91a989dce9830543"
subsystem = "hydracache-server"
owning_type = "Vec"
source = "crates/hydracache-server/src/config.rs"
symbol = "ServerConfig"
allocation_sites = ["seeds"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ServerConfig"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-92753920b5247d68"
subsystem = "hydracache"
owning_type = "JoinHandle + Sender"
source = "crates/hydracache/src/cluster/admission.rs"
symbol = "ClusterAdmissionBridgeHandle"
allocation_sites = ["shutdown", "task"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterAdmissionBridgeHandle"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-92753920b5247d68.count", "owner-92753920b5247d68.bytes", "owner-92753920b5247d68.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-934d422a46cc5eec"
subsystem = "hydracache"
owning_type = "BTreeMap + Vec"
source = "crates/hydracache/src/grid/checkpoint.rs"
symbol = "ClusterCheckpointManifest"
allocation_sites = ["partition_watermarks", "node_manifests"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterCheckpointManifest"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-9393f9a91fa8fbb8"
subsystem = "hydracache-client-hc2"
owning_type = "Bytes + Sender + Weak"
source = "crates/hydracache-client-hc2/src/reconnect.rs"
symbol = "SubscriptionState"
allocation_sites = ["key_prefix", "events", "runtime"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate SubscriptionState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-9393f9a91fa8fbb8.count", "owner-9393f9a91fa8fbb8.bytes", "owner-9393f9a91fa8fbb8.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-95a0fb34083cdee0"
subsystem = "hydracache-cluster-chitchat"
owning_type = "BTreeMap + VecDeque"
source = "crates/hydracache-cluster-chitchat/src/lib.rs"
symbol = "DiscoveryState"
allocation_sites = ["candidates", "liveness", "events"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate DiscoveryState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-95a0fb34083cdee0.count", "owner-95a0fb34083cdee0.bytes", "owner-95a0fb34083cdee0.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-95b86af7ca5a86ea"
subsystem = "hydracache-server"
owning_type = "Arc"
source = "crates/hydracache-server/src/grid_host.rs"
symbol = "SnapshotSenderTaskMetrics"
allocation_sites = ["current", "high_water"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate SnapshotSenderTaskMetrics"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-9751f583a69c4338"
subsystem = "hydracache-server"
owning_type = "Vec"
source = "crates/hydracache-server/src/cluster_status.rs"
symbol = "ClusterStatus"
allocation_sites = ["members", "voter_ids"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterStatus"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-9c6209a09fa4367c"
subsystem = "hydracache-observability"
owning_type = "Vec"
source = "crates/hydracache-observability/src/lib.rs"
symbol = "ClusterStatus"
allocation_sites = ["members"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterStatus"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-9d5849e0710a6864"
subsystem = "hydracache"
owning_type = "Arc + Vec"
source = "crates/hydracache/src/testing.rs"
symbol = "StagingClusterHarness"
allocation_sites = ["cluster", "member_caches", "client_caches"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate StagingClusterHarness"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-9d5849e0710a6864.count", "owner-9d5849e0710a6864.bytes", "owner-9d5849e0710a6864.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-a121db93921ed3f6"
subsystem = "hydracache-core"
owning_type = "Vec"
source = "crates/hydracache-core/src/events.rs"
symbol = "CacheEvent"
allocation_sites = ["tags"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheEvent"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-a288ed35c479defa"
subsystem = "hydracache-actuator-axum"
owning_type = "Vec"
source = "crates/hydracache-actuator-axum/src/lib.rs"
symbol = "ClusterPilotReportResponse"
allocation_sites = ["caches", "highlights"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterPilotReportResponse"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-a8d1f5de9e73e045"
subsystem = "hydracache"
owning_type = "HashMap"
source = "crates/hydracache/src/load_breaker.rs"
symbol = "LoadBreakerRegistry"
allocation_sites = ["entries"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate LoadBreakerRegistry"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-a8d1f5de9e73e045.count", "owner-a8d1f5de9e73e045.bytes", "owner-a8d1f5de9e73e045.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-a91d41c1c403c0a2"
subsystem = "hydracache-macros"
owning_type = "Vec"
source = "crates/hydracache-macros/src/cacheable.rs"
symbol = "CacheableAttributeConfig"
allocation_sites = ["repeated_tags", "tag_segments"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheableAttributeConfig"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-ae229d985a858cf6"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/hardening.rs"
symbol = "InMemoryReplicatedValueStore"
allocation_sites = ["records"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryReplicatedValueStore"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-af6b327b0e909dfd"
subsystem = "hydracache-client-hc2"
owning_type = "Vec"
source = "crates/hydracache-client-hc2/src/types.rs"
symbol = "ClientConfig"
allocation_sites = ["capabilities"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClientConfig"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-aff524598a556ad0"
subsystem = "hydracache-client-hc2"
owning_type = "Bytes + Weak"
source = "crates/hydracache-client-hc2/src/client.rs"
symbol = "FencedSession"
allocation_sites = ["runtime", "id"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate FencedSession"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-aff524598a556ad0.count", "owner-aff524598a556ad0.bytes", "owner-aff524598a556ad0.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-b0483e094937f540"
subsystem = "hydracache"
owning_type = "BTreeMap + Vec"
source = "crates/hydracache/src/cluster/discovery.rs"
symbol = "InMemoryClusterDiscoveryState"
allocation_sites = ["candidates", "events", "liveness"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryClusterDiscoveryState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-b0483e094937f540.count", "owner-b0483e094937f540.bytes", "owner-b0483e094937f540.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-b1aabc6d88467a4c"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/grid/checkpoint.rs"
symbol = "NodeCheckpointManifest"
allocation_sites = ["snapshots"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate NodeCheckpointManifest"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-b49d06f2430952fb"
subsystem = "hydracache-server"
owning_type = "Arc + BTreeSet"
source = "crates/hydracache-server/src/grid_host.rs"
symbol = "SnapshotDeliveryFeedback"
allocation_sites = ["raft", "diagnostics", "in_flight_peers"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate SnapshotDeliveryFeedback"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-b4e765bb956a5c31"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/cluster/diagnostics.rs"
symbol = "ClusterDiscoveryDiagnostics"
allocation_sites = ["candidates", "events"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterDiscoveryDiagnostics"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-b4e765bb956a5c31.count", "owner-b4e765bb956a5c31.bytes", "owner-b4e765bb956a5c31.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-b5bc682d8dd05786"
subsystem = "hydracache"
owning_type = "Arc + VecDeque"
source = "crates/hydracache/src/invalidation_transport.rs"
symbol = "BoundedFrameQueue"
allocation_sites = ["inner", "notify"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate BoundedFrameQueue"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-b62cc6c8c03a9c29"
subsystem = "hydracache-server"
owning_type = "Arc"
source = "crates/hydracache-server/src/bootstrap.rs"
symbol = "ServerRuntime"
allocation_sites = ["client_dispatch_state", "cluster_status", "grid_control"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ServerRuntime"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-b62cc6c8c03a9c29.count", "owner-b62cc6c8c03a9c29.bytes", "owner-b62cc6c8c03a9c29.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-b72e8da53ca9daa0"
subsystem = "hydracache"
owning_type = "BTreeMap + Vec"
source = "crates/hydracache/src/cluster/admission.rs"
symbol = "ClusterAdmissionBridgeState"
allocation_sites = ["admitted", "events"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterAdmissionBridgeState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-b72e8da53ca9daa0.count", "owner-b72e8da53ca9daa0.bytes", "owner-b72e8da53ca9daa0.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-b777e267dd667f15"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/grid/invalidation_ring.rs"
symbol = "InvalidationRingSnapshot"
allocation_sites = ["events"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InvalidationRingSnapshot"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-b7e51d6c659907a8"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/cluster/membership.rs"
symbol = "ClusterCandidate"
allocation_sites = ["metadata"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterCandidate"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-b99b12df57d6835d"
subsystem = "hydracache-observability"
owning_type = "Vec"
source = "crates/hydracache-observability/src/lib.rs"
symbol = "HydraCacheOverview"
allocation_sites = ["caches"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraCacheOverview"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-bc156f91b2a47e74"
subsystem = "hydracache"
owning_type = "Arc"
source = "crates/hydracache/src/cluster/control_plane.rs"
symbol = "InMemoryCluster"
allocation_sites = ["invalidation_bus"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemoryCluster"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-bc156f91b2a47e74.count", "owner-bc156f91b2a47e74.bytes", "owner-bc156f91b2a47e74.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-bcf69f38db656350"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/grid/merkle_repair.rs"
symbol = "RepairSession"
allocation_sites = ["peers"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RepairSession"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-bcf69f38db656350.count", "owner-bcf69f38db656350.bytes", "owner-bcf69f38db656350.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-bdbff7278a827e13"
subsystem = "hydracache-cluster-raft"
owning_type = "Arc"
source = "crates/hydracache-cluster-raft/src/log_store.rs"
symbol = "DurableRaftLogDirectory"
allocation_sites = ["format_version", "fsync_count"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate DurableRaftLogDirectory"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "file-format and configured retention bound"
byte_bound = "configured durable retention and filesystem capacity"
age_bound = "configured checkpoint/log retention lifecycle"
overflow_behavior = "fail-loud before exceeding durable retention or storage limits"
snapshot_fields = ["owner-bdbff7278a827e13.file_bytes", "owner-bdbff7278a827e13.records"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "file_backed"
disposition_reason = "Retention is durable/file-backed and is accounted separately from anonymous live heap."

[[owner]]
owner_id = "owner-c2827ba9e35f433b"
subsystem = "hydracache-cluster-raft"
owning_type = "Vec"
source = "crates/hydracache-cluster-raft/src/lib.rs"
symbol = "RaftMetadataRuntimeConfig"
allocation_sites = ["voters"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftMetadataRuntimeConfig"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-c3c8d9a77b32e83f"
subsystem = "hydracache"
owning_type = "HashMap"
source = "crates/hydracache/src/invalidation_transport.rs"
symbol = "InboundApplyState"
allocation_sites = ["highest_generation", "rate_limited_by_source"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InboundApplyState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-c3c8d9a77b32e83f.count", "owner-c3c8d9a77b32e83f.bytes", "owner-c3c8d9a77b32e83f.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-c6c5bc5f2afdd123"
subsystem = "hydracache-client-hc2"
owning_type = "Arc"
source = "crates/hydracache-client-hc2/src/client.rs"
symbol = "ActiveSession"
allocation_sites = ["fence"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ActiveSession"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-c6c5bc5f2afdd123.count", "owner-c6c5bc5f2afdd123.bytes", "owner-c6c5bc5f2afdd123.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-c96ddfe3d06baf65"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/grid/elasticity.rs"
symbol = "InMemorySnapshotSink"
allocation_sites = ["snapshots"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InMemorySnapshotSink"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-ca1c49adf21453ef"
subsystem = "hydracache-server"
owning_type = "Vec"
source = "crates/hydracache-server/src/bootstrap.rs"
symbol = "ServerAdminStatus"
allocation_sites = ["member_ids", "voter_ids"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ServerAdminStatus"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-cb74ddab28081ff3"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/multitenancy.rs"
symbol = "TenantMetricsSnapshot"
allocation_sites = ["tenant_bytes", "tenant_entries", "tenant_admission_rejected_total", "tenant_namespace_bytes", "tenant_namespace_entries", "tenant_namespace_quota_bytes", "tenant_namespace_quota_entries", "tenant_request_count", "tenant_rate_limit_per_window", "tenant_fair_share_count", "tenant_fair_share_per_window", "tenant_subscriptions", "tenant_max_subscriptions"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate TenantMetricsSnapshot"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-cc7269ea73141ecf"
subsystem = "hydracache-server"
owning_type = "Vec"
source = "crates/hydracache-server/src/grid_host.rs"
symbol = "TestRaftOutboundFaultDocument"
allocation_sites = ["rules"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate TestRaftOutboundFaultDocument"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-cc7269ea73141ecf.count", "owner-cc7269ea73141ecf.bytes", "owner-cc7269ea73141ecf.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-ccd1816a71d9f6e9"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/tag_index.rs"
symbol = "LoadGenerationSnapshot"
allocation_sites = ["tags"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate LoadGenerationSnapshot"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-cd5e5e980d574899"
subsystem = "hydracache-client-transport-axum"
owning_type = "Arc"
source = "crates/hydracache-client-transport-axum/src/lib.rs"
symbol = "ClientSurfaceRuntime"
allocation_sites = ["state"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClientSurfaceRuntime"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-cd5e5e980d574899.count", "owner-cd5e5e980d574899.bytes", "owner-cd5e5e980d574899.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-cdf7c27c33f78037"
subsystem = "hydracache"
owning_type = "BTreeMap + Vec"
source = "crates/hydracache/src/grid/mod.rs"
symbol = "ClusterGridDiagnostics"
allocation_sites = ["partition_replica_versions"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterGridDiagnostics"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-cdf7c27c33f78037.count", "owner-cdf7c27c33f78037.bytes", "owner-cdf7c27c33f78037.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-d80620ca25568c39"
subsystem = "hydracache"
owning_type = "Arc"
source = "crates/hydracache/src/cache.rs"
symbol = "HydraCache"
allocation_sites = ["inner"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraCache"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-d80620ca25568c39.count", "owner-d80620ca25568c39.bytes", "owner-d80620ca25568c39.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-d88dfff9096cdbe5"
subsystem = "hydracache-client-protocol"
owning_type = "BTreeMap"
source = "crates/hydracache-client-protocol/src/java_migration.rs"
symbol = "JavaCodecRegistryContract"
allocation_sites = ["codecs"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate JavaCodecRegistryContract"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-d88dfff9096cdbe5.count", "owner-d88dfff9096cdbe5.bytes", "owner-d88dfff9096cdbe5.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-d8dab3d3916ec91e"
subsystem = "hydracache-db"
owning_type = "Vec"
source = "crates/hydracache-db/src/policy.rs"
symbol = "QueryCachePolicy"
allocation_sites = ["required_dimensions", "key_dimension_labels", "tag_dimension_labels", "dimension_allow"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate QueryCachePolicy"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-d8dab3d3916ec91e.count", "owner-d8dab3d3916ec91e.bytes", "owner-d8dab3d3916ec91e.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-db56e78d38567630"
subsystem = "hydracache"
owning_type = "JoinHandle"
source = "crates/hydracache/src/events.rs"
symbol = "CacheEventListenerHandle"
allocation_sites = ["task"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheEventListenerHandle"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-dcc916ff769b124b"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/cluster/control_plane.rs"
symbol = "RaftMetadataState"
allocation_sites = ["commands"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftMetadataState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-dcc916ff769b124b.count", "owner-dcc916ff769b124b.bytes", "owner-dcc916ff769b124b.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-e33db23c4f497efb"
subsystem = "hydracache-cluster-raft"
owning_type = "Arc"
source = "crates/hydracache-cluster-raft/src/lib.rs"
symbol = "RaftMetadataRuntime"
allocation_sites = ["cluster", "metadata_store"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RaftMetadataRuntime"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-e33db23c4f497efb.count", "owner-e33db23c4f497efb.bytes", "owner-e33db23c4f497efb.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-e47b54027941b13f"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/lock_session.rs"
symbol = "SessionHeartbeats"
allocation_sites = ["last_seen"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate SessionHeartbeats"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-e47b54027941b13f.count", "owner-e47b54027941b13f.bytes", "owner-e47b54027941b13f.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-e684ac1080e3c2eb"
subsystem = "hydracache-cluster-transport-axum"
owning_type = "Arc + BTreeMap"
source = "crates/hydracache-cluster-transport-axum/src/lib.rs"
symbol = "ClusterRouteAuth"
allocation_sites = ["identity", "authorizer", "rejected_total", "rejected_by_route"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterRouteAuth"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-e684ac1080e3c2eb.count", "owner-e684ac1080e3c2eb.bytes", "owner-e684ac1080e3c2eb.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-e78511f2a5b9eb2d"
subsystem = "hydracache-client-hc2"
owning_type = "Sender"
source = "crates/hydracache-client-hc2/src/client.rs"
symbol = "PendingSubscription"
allocation_sites = ["events", "reply"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate PendingSubscription"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-e78511f2a5b9eb2d.count", "owner-e78511f2a5b9eb2d.bytes", "owner-e78511f2a5b9eb2d.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-e87749f0f74850b6"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/grid/mod.rs"
symbol = "ReplicationPayload"
allocation_sites = ["bytes"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ReplicationPayload"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-ea443f37a2b9b5ec"
subsystem = "hydracache-client-protocol"
owning_type = "Bytes"
source = "crates/hydracache-client-protocol/src/lib.rs"
symbol = "ClientFrame"
allocation_sites = ["payload"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClientFrame"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-ec7479176cf07f1d"
subsystem = "hydracache-client-hc2"
owning_type = "Sender"
source = "crates/hydracache-client-hc2/src/client.rs"
symbol = "PendingInvocation"
allocation_sites = ["reply"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate PendingInvocation"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-ec7479176cf07f1d.count", "owner-ec7479176cf07f1d.bytes", "owner-ec7479176cf07f1d.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-ee6f8028c0848829"
subsystem = "hydracache-observability"
owning_type = "Vec"
source = "crates/hydracache-observability/src/lib.rs"
symbol = "ClusterOverview"
allocation_sites = ["members"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterOverview"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-ee8beba145af9dce"
subsystem = "hydracache"
owning_type = "BTreeMap + VecDeque"
source = "crates/hydracache/src/cluster/node.rs"
symbol = "ClusterNode"
allocation_sites = ["outbound", "storage_requests", "storage_results"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterNode"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-ee8beba145af9dce.count", "owner-ee8beba145af9dce.bytes", "owner-ee8beba145af9dce.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-eec05bf903ed7f53"
subsystem = "hydracache-client-hc2"
owning_type = "Arc"
source = "crates/hydracache-client-hc2/src/reconnect.rs"
symbol = "RecoveringFencedSession"
allocation_sites = ["state"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RecoveringFencedSession"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-eec05bf903ed7f53.count", "owner-eec05bf903ed7f53.bytes", "owner-eec05bf903ed7f53.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-ef361f6909175f64"
subsystem = "hydracache"
owning_type = "Arc"
source = "crates/hydracache/src/builder.rs"
symbol = "HydraCacheBuilder"
allocation_sites = ["invalidation_bus"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraCacheBuilder"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-f0b411a22a761278"
subsystem = "hydracache-observability"
owning_type = "Arc + BTreeMap"
source = "crates/hydracache-observability/src/lib.rs"
symbol = "HydraCacheRegistry"
allocation_sites = ["probes"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate HydraCacheRegistry"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-f0b411a22a761278.count", "owner-f0b411a22a761278.bytes", "owner-f0b411a22a761278.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-ef32b93dd02f43ec"
subsystem = "hydracache"
owning_type = "Vec"
source = "crates/hydracache/src/memory_footprint.rs"
symbol = "MemoryFootprintSnapshot"
allocation_sites = ["owners", "subsystem_versions"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "one bounded snapshot owner observation vector"
creation_transition = "construct a diagnostic or evidence snapshot"
terminal_transitions = ["snapshot serialization completes", "snapshot drop"]
cleanup = "owned snapshot vector is released on Rust Drop"
count_bound = "constant: at most 32 owner observations by schema"
byte_bound = "constant owner observation fields plus bounded owner ids"
age_bound = "scope: diagnostic request or evidence checkpoint lifetime"
overflow_behavior = "snapshot creation fails before accepting an unbounded owner set"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_profile_071::loadgen_declares_the_exact_phase_order_and_epoch_fields"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The vector is the bounded serialized snapshot itself and is released after the diagnostic/artifact checkpoint."

[[owner]]
owner_id = "owner-f184fb44837b0512"
subsystem = "hydracache"
owning_type = "HashMap + HashSet"
source = "crates/hydracache/src/tag_index.rs"
symbol = "TagIndexState"
allocation_sites = ["keys_by_tag", "generations", "key_generations"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate TagIndexState"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-f184fb44837b0512.count", "owner-f184fb44837b0512.bytes", "owner-f184fb44837b0512.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-f23f0921dd869d1f"
subsystem = "hydracache-redis-compat"
owning_type = "BTreeMap + BTreeSet + Vec"
source = "crates/hydracache-redis-compat/src/lib.rs"
symbol = "RedisTagIndex"
allocation_sites = ["key_tags", "tag_keys"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RedisTagIndex"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-f23f0921dd869d1f.count", "owner-f23f0921dd869d1f.bytes", "owner-f23f0921dd869d1f.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-f79902521df1bcc0"
subsystem = "hydracache"
owning_type = "BTreeMap"
source = "crates/hydracache/src/grid/mod.rs"
symbol = "PartitionReplicaVersions"
allocation_sites = ["versions"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate PartitionReplicaVersions"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-f79902521df1bcc0.count", "owner-f79902521df1bcc0.bytes", "owner-f79902521df1bcc0.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-f8140e6f623475e5"
subsystem = "hydracache"
owning_type = "Arc"
source = "crates/hydracache/src/cluster/admission.rs"
symbol = "ClusterAdmissionBridgeInner"
allocation_sites = ["discovery", "control_plane"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate ClusterAdmissionBridgeInner"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-f8140e6f623475e5.count", "owner-f8140e6f623475e5.bytes", "owner-f8140e6f623475e5.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-fa64d75430a961e5"
subsystem = "hydracache-client-hc2"
owning_type = "Arc"
source = "crates/hydracache-client-hc2/src/reconnect.rs"
symbol = "RecoveringHc2Client"
allocation_sites = ["handle"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate RecoveringHc2Client"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-fa64d75430a961e5.count", "owner-fa64d75430a961e5.bytes", "owner-fa64d75430a961e5.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-fc48908ad1404888"
subsystem = "hydracache-core"
owning_type = "Vec"
source = "crates/hydracache-core/src/key.rs"
symbol = "CacheKeyBuilder"
allocation_sites = ["segments"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheKeyBuilder"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-fd2fb3a914043787"
subsystem = "hydracache-client-hc2"
owning_type = "Arc"
source = "crates/hydracache-client-hc2/src/reconnect.rs"
symbol = "CurrentSnapshot"
allocation_sites = ["client"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CurrentSnapshot"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-fdb80d329d653362"
subsystem = "hydracache"
owning_type = "Arc + SharedLoadFuture"
source = "crates/hydracache/src/inflight.rs"
symbol = "InFlightEntry"
allocation_sites = ["load", "waiters"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate InFlightEntry"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-fdb80d329d653362.count", "owner-fdb80d329d653362.bytes", "owner-fdb80d329d653362.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."

[[owner]]
owner_id = "owner-fe69cd6ffe0c1209"
subsystem = "hydracache-actuator-axum"
owning_type = "Vec"
source = "crates/hydracache-actuator-axum/src/lib.rs"
symbol = "CacheList"
allocation_sites = ["caches"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate CacheList"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "scope: owning value lifetime"
byte_bound = "scope: aggregate payload released on drop"
age_bound = "scope: no lifetime beyond aggregate or operation"
overflow_behavior = "operation fails before an unbounded retained owner is created"
snapshot_fields = []
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "ephemeral"
disposition_reason = "The discovered allocation belongs to a request, response, snapshot, configuration, event, or operation aggregate and is released with that value."

[[owner]]
owner_id = "owner-ff665a12831693a8"
subsystem = "hydracache-cluster-transport-axum"
owning_type = "BTreeMap + OwnerLoadHandler"
source = "crates/hydracache-cluster-transport-axum/src/lib.rs"
symbol = "OwnerLoadRegistry"
allocation_sites = ["loaders"]
keying_dimension = "aggregate-only; runtime keys, tenants, sessions, and connections are forbidden metric labels"
retained_unit = "logical elements and bytes owned by the listed fields"
creation_transition = "construct or activate OwnerLoadRegistry"
terminal_transitions = ["explicit cleanup/reset/expiry/disconnect where supported", "owner drop", "cancellation and unwind"]
cleanup = "subsystem terminal transition plus Rust Drop; focused lifecycle tests must prove both"
count_bound = "config: subsystem-specific finite count or protocol quota"
byte_bound = "config: finite count multiplied by retained-unit maximum"
age_bound = "lifecycle: explicit cleanup, reset, expiry, disconnect, or owner drop"
overflow_behavior = "reject, evict, coalesce, or backpressure before allocation; never fail open"
snapshot_fields = ["owner-ff665a12831693a8.count", "owner-ff665a12831693a8.bytes", "owner-ff665a12831693a8.oldest_age_seconds"]
security_classification = "aggregate-sensitive; no payloads or unbounded labels"
focused_tests = ["memory_ownership_071::registry_entry_resolves"]
slow_evidence_ids = []
disposition = "bounded"
disposition_reason = "Conservative AST discovery identified a lifecycle-bearing production aggregate; W1 reconciles its bounded aggregate counters."
