schema_version = 1
release = "0.73"
contract_id = "observer-baseline-pilot-073-v2"
state = "preregistered-unmeasured"
supersedes = "docs/testing/performance/0.73/baseline-pilot-contract.toml"
triggering_evidence = "docs/testing/performance/0.73/baseline-pilot-insufficient-862c9015.toml"
evidence_class = "dedicated_host_baseline_only"
host_admission = "docs/testing/performance/0.73/host-admission-3ba09fcc.toml"
candidate_data_allowed = false
promotable = false
numerical_claim_eligible = false
thresholds_changed = false
workload_changed = false
offered_rates_changed = false
instrumentation_modes = ["off", "production"]
offered_rates_per_second = [2500, 5000, 10000, 20000]
repeats_per_rate_and_mode = 5
window_seconds = 10
warmup_operations = 5000
run_order_seed = 73073
counterbalanced_order_required = true
independent_processes_required = true
complete_outcome_accounting_required = true
exact_reconciliation_required = true
paired_estimator = "hodges-lehmann-v1"
minimum_stable_rates = 3
minimum_achieved_ratio = 0.98
maximum_goodput_relative_spread = 0.15
p99_slo_microseconds = 10000
maximum_goodput_regression = 0.02
maximum_cpu_per_operation_regression = 0.03
maximum_p99_regression = 0.03
stable_load_fractions = [0.25, 0.60, 0.85]
selection_rule = "For each repeat, compute production-versus-off relative goodput, CPU/operation, and p99 differences before aggregation. Apply the one-sample Hodges-Lehmann estimator to the five paired differences. Select the highest preregistered rate for which both modes and the paired estimates pass every unchanged guard; derive D3 rates as 25%, 60%, and 85% rounded to 100 operations/second."
failure_rule = "Retain every attempt and do not silently retry. Fewer than three stable rates leaves I73 unfrozen. V2 may reject or admit the baseline but cannot erase any v1 result."
workflow_trigger = "manual-only-protected-environment"

[workload_mix_percent]
get = 40
tagged_put = 25
remove_refill = 15
tag_invalidate_refill = 10
ttl_put = 10
