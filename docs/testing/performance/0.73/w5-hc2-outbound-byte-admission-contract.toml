schema_version = 1
release = "0.73"
contract_id = "w5-hc2-outbound-byte-admission-073-v1"
state = "preregistered-before-candidate-implementation"
triggering_evidence = "docs/testing/performance/0.73/w5-hc2-raw-transport-stall-25cdfb61.toml"
source_parent = "d715a87d0f1e52eda1417887f622f3ceafc2642f"
implementation_scope = ["crates/hydracache-server/src/hc2.rs", "crates/hydracache-server/Cargo.toml"]
application_queue_byte_budget = 1048576
application_queue_item_capacity_source = "ClientSurfaceLimits.max_streams_per_connection"
default_application_queue_item_capacity = 16
charge_measure = "prost::Message::encoded_len(ServerEnvelope)"
oversize_policy = "One legal envelope whose encoded length meets or exceeds the budget acquires the entire budget; it is never rejected solely by this admission layer."
permit_lifetime = "The owned byte permit is retained by the queued wrapper and released when tonic polls the item or the queue/connection is dropped."
single_producer_bound = "The connection task is the only producer, so at most one already-constructed frame can wait outside the byte-admitted queue."
public_configuration_changed = false
wire_contract_changed = false
client_api_changed = false
production_counter_addition = false
item_capacity_changed = false
dispatch_semantics_changed = false
frame_rejection_added = false
http2_window_claimed = false
tls_or_socket_bytes_claimed = false
dedicated_host_run_allowed = false
release_numerical_claim_allowed = false
promotable = false
candidate_implementation_allowed = true
encoded_length_charging_required = true
oversize_progress_required = true
permit_release_on_send_failure_required = true
permit_release_on_disconnect_required = true
existing_item_bound_required = true
existing_ordering_required = true
existing_backpressure_required = true
existing_close_reconciliation_required = true
local_raw_matrix_required = true
value_bytes = [4096, 262144]
pairs_per_value_size = 5
connections = 8
offered_value_bytes_per_connection = 8388608
candidate_success_rule = "All unit, real-mTLS, contract, and documentation gates pass; every raw-matrix invariant and close reconciliation passes; the 4 KiB cell retains its item-bound behavior; and the 256 KiB unpolled median dispatch and paired server retention both decrease relative to the retained 25cdfb61 evidence without changing drained completion."
candidate_falsifier = "Reject the candidate on changed wire/public configuration, message rejection, reordered frames, permit leakage, an oversize deadlock, failure to reconcile after disconnect, any raw attempt failure/stderr, incomplete drained control, unstable treatment dispatch, or no directional improvement in both high-payload stalled dispatch and paired server retention. Preserve failed attempts without silent replacement."
claim_boundary = "The candidate bounds encoded bytes waiting in HydraCache's application mpsc queue. It does not bound the producer-held frame, an item already polled by tonic, Hyper/h2 flow-control state, protobuf/TLS work buffers, kernel socket buffers, allocator high-water pages, or peer memory."
rollback = "Remove the byte semaphore and queued permit wrapper, restore ReceiverStream<Result<ServerEnvelope, Status>>, and retain the existing 16-item channel if correctness, progress, cleanup, or the frozen local direction fails."
next_decision = "If locally accepted, retain the candidate for the later integrated W5/W2 baseline-v2 campaign; do not buy a dedicated-host run for this subproblem alone."
