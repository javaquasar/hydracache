schema_version = 1
release = "0.73"
contract_id = "observer-cpu-attribution-073-v1"
state = "preregistered-unmeasured"
evidence_class = "dedicated_host_diagnostic_only"
profile_id = "performance-reference-073-v1"
host_admission = "docs/testing/performance/0.73/host-admission-3ba09fcc.toml"
triggering_evidence = "docs/testing/performance/0.73/baseline-pilot-insufficient-4ba93a1a.toml"
candidate_data_allowed = false
candidate_measurement_authorized = false
promotable = false
numerical_claim_eligible = false
thresholds_changed = false
acceptance_decision_allowed = false
offered_rate_per_second = 10000
repeats_per_mode = 5
window_seconds = 5
warmup_operations = 5000
run_order_seed = 73110
counterbalanced_order_required = true
independent_processes_required = true
complete_outcome_accounting_required = true
instrumentation_modes = ["off", "counters-only", "observer-noop", "production"]
comparison_chain = ["off_to_counters_only", "counters_only_to_observer_noop", "observer_noop_to_production", "off_to_production"]
interpretation_rule = "Use adjacent median deltas only to locate CPU and allocation ownership. Counters-only and observer-noop deliberately violate complete removal accounting and cannot support correctness, release, baseline-freeze, or acceptance claims."
completion_rule = "Retain all 20 independent attempts. The diagnostic completes when every attempt has complete outcome accounting and the admitted host passes unchanged pre/post identity and calibration checks; observed deltas cannot make the workflow fail or pass an acceptance gate."
next_decision = "Use the attributed owner to choose a local implementation optimization, then rerun deterministic correctness and cheap local screens before repeating the unchanged baseline-only admission contract."
