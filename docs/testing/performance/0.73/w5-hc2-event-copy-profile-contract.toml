schema_version = 1
release = "0.73"
contract_id = "w5-hc2-event-copy-profile-073-v1"
state = "preregistered-awaiting-local-profile"
parent_contract = "docs/testing/performance/0.73/w1-owner-classification-contract.toml"
source_parent = "9bcafa654281a4ba56d93972e8d5db16096a99ee"
source_owner = "crates/hydracache-server/src/hc2.rs::emit_matching_events"
profile_tool = "tools/hc2-event-copy-profile-073"
production_counter_addition = false
product_mutation_allowed = false
candidate_data_allowed = false
dedicated_host_run_allowed = false
local_results_promotable = false
independent_processes_required = true
repeats_per_scenario = 5
key_bytes = 64
fanouts = [1, 8, 16]
value_bytes = [128, 4096]
scenarios = ["fanout-1-value-128", "fanout-8-value-128", "fanout-16-value-128", "fanout-16-value-4096"]
queue_capacity_owner = "tokio mpsc per HC/2 connection, capacity max_streams_per_connection (default 16), plus the producer-held frame blocked on send"
copy_owner = "CacheEvent construction uses Bytes::copy_from_slice for both key and value for every matching subscription; mutation_event already owns immutable reference-counted Bytes"
hypothesis = "With frame-vector capacity prepared outside measurement, allocation count equals two allocations per subscriber and gross bytes equal fanout * (key_bytes + value_bytes)."
falsifier = "Reject attribution if any process fails, frames retained differ from fanout, allocation count is not 2 * fanout, gross bytes differ from logical copied bytes, or scaling is not exactly linear."
interpretation_limit = "This local primitive attributes event-payload copies. It does not measure tonic buffers, TLS records, task allocation, mutex wait, CPU, latency, RSS, or release capacity."
next_decision = "If confirmed, preregister a D2 candidate that passes immutable Bytes through emit_matching_events and shallow-clones them into each CacheEvent while preserving wire bytes, ordering, bounded backpressure, and disconnect reconciliation."
