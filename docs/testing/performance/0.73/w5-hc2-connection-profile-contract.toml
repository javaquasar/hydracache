schema_version = 1
release = "0.73"
contract_id = "w5-hc2-connection-profile-073-v1"
state = "preregistered-awaiting-local-process-matrix"
triggering_evidence = "docs/testing/performance/0.73/w5-hc2-connection-census-e673f9cd.toml"
source_parent = "b77d061c72a51eaae17e87bc16dbccde4b4e2862"
profile_tool = "tools/hc2-connection-profile-073"
ownership_scope = "combined-local-client-server-process"
cardinalities = [1, 10, 100, 1000]
repeats_per_cardinality = 3
independent_processes_required = true
metrics = ["gross_allocated_bytes", "working_set_bytes", "peak_working_set_bytes", "pagefile_bytes", "peak_pagefile_bytes"]
production_counter_addition = false
candidate_data_allowed = false
dedicated_host_run_allowed = false
promotable = false
per_server_connection_claim_allowed = false
release_numerical_claim_allowed = false
falsifier = "Reject any process with logical counter mismatch, client retained resources, incomplete close reconciliation, or missing OS memory fields. Preserve nonlinear and negative deltas rather than forcing a per-connection fit."
interpretation_limit = "The process contains both local clients and server. Allocation and OS memory include TLS, HTTP/2, runtime, allocator, and both endpoints. Results may reveal scaling shape but cannot be presented as server bytes per connection."
next_decision = "Use three independent processes per cardinality to decide whether a server/client split probe is justified; do not optimize from RSS alone."
