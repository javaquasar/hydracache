schema_version = 1
release = "0.73"
contract_id = "w5-hc2-slow-consumer-profile-073-v1"
state = "preregistered-awaiting-local-drained-vs-unread-pairs"
triggering_evidence = "docs/testing/performance/0.73/w5-hc2-split-connection-profile-19a440d4.toml"
source_parent = "a653dddf4d694e15917862763ff49417b1ad44fc"
profile_tool = "tools/hc2-connection-profile-073"
connections = 100
subscriptions_per_connection = 1
mutations_per_connection = 1100
total_mutations = 110000
value_bytes = 128
client_subscription_item_capacity = 1024
server_outbound_item_capacity = 16
scenarios = ["drained-application-consumers", "unread-application-consumers"]
pairs = 5
counterbalanced_order = ["drained/unread", "unread/drained", "drained/unread", "unread/drained", "drained/unread"]
independent_process_pairs_required = true
unique_prefix_per_connection_required = true
same_mutation_volume_required = true
transport_reader_remains_active = true
production_counter_addition = false
product_mutation_allowed = false
candidate_data_allowed = false
dedicated_host_run_allowed = false
promotable = false
server_queued_byte_claim_allowed = false
release_numerical_claim_allowed = false
control_invariant = "All 110,000 mutations complete; all 100 subscriptions remain active at pressure snapshot; continuously drained consumers report zero dropped events."
treatment_invariant = "All 110,000 mutations complete; all 100 subscriptions remain active at pressure snapshot; unread consumers report dropped events after bounded client subscription queues saturate."
close_invariant = "After subscriptions and clients close, server connections/subscriptions/sessions/invocations reconcile to zero and accepted equals closed."
falsifier = "Reject a pair on process failure or stderr, incomplete mutations, wrong subscription cardinality, any control drop, no treatment drop, missing endpoint snapshot, or incomplete close reconciliation. Preserve all failed pairs without replacement."
interpretation_limit = "This is an application-consumer stall: the HC/2 client transport reader continues draining HTTP/2 and applies bounded local drop/gap repair. It measures client subscription retention and its server-side consequences, not a peer that stops reading the network and not exact server queued bytes."
next_decision = "If unread-minus-drained retention is bounded and predominantly client-owned, close the application slow-consumer hypothesis locally and require a separately preregistered raw-transport probe before any server queue-byte candidate."
