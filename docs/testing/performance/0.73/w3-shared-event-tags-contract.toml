schema_version = 1
release = "0.73"
contract_id = "w3-shared-event-tags-073-v1"
state = "preregistered-before-product-mutation"
parent_contract = "docs/testing/performance/0.73/w3-tag-index-profile-contract.toml"
baseline_evidence = "docs/testing/performance/0.73/w3-tag-index-profile-f8b90ef0.toml"
source_parent = "c213bc1bdf19fb29562241d71ea6ef4a28acd055"
candidate_scope = "change CacheEvent tag ownership from Vec<String> to one immutable shared slice while retaining content-based public behavior"
candidate_files = ["crates/hydracache-core/src/events.rs"]
entry_count = 256
key_bytes = 32
tag_bytes = 32
tag_cardinalities = [0, 1, 4, 16, 64]
tag_topologies = ["shared-tag-set", "unique-tag-set"]
event_subscriber_counts = [0, 1, 8]
invalidation_fanouts = [1, 64, 1024]
repeats_per_scenario = 5
candidate_attempts = 215
independent_processes_required = true
prebuilt_release_binary_required = true
exact_memory_reconciliation_required = true
event_content_equality_required = true
generation_fence_required = true
public_tags_accessor_unchanged = true
event_equality_semantics_unchanged = true
product_mutation_allowed = true
production_counter_addition = false
dedicated_host_run_allowed = false
promotable = false
release_numerical_claim_allowed = false
elapsed_time_is_acceptance_metric = false
primary_cell = "event/shared-tag-set/64-tags/8-subscribers"
baseline_primary_median_gross_allocated_bytes = 15628164
minimum_primary_total_gross_reduction_fraction = 0.30
baseline_primary_tag_only_gross_increment_bytes = 7340064
maximum_candidate_tag_churn_bytes_per_delivery_tag = 12.0
maximum_no_subscriber_gross_regression_fraction = 0.05
maximum_index_live_delta_regression_fraction = 0.10
maximum_invalidation_gross_regression_fraction = 0.10
falsifier = "Reject and retain the candidate if any of 215 processes fails, stderr is non-empty, exact reconciliation or event contents differ, a delivery is missing or duplicated, stale-load fencing fails, invalidation removes the wrong fan-out, the primary gross reduction is below 30%, per-delivery tag churn exceeds 12 bytes, or no-subscriber/index/invalidation controls cross their frozen regression limits."
interpretation_limit = "This local comparison can accept or reject ownership sharing as a release candidate but cannot establish portable latency, capacity, RSS, or release-grade percentage claims. Elapsed time is diagnostic only."
decision_rule = "Accept locally only when the primary allocation threshold and every semantic/control gate pass across the full matrix. Otherwise revert the product mutation and retain both baseline and failed candidate evidence."
next_decision = "Implement the one-field ownership change with focused core tests, rerun the full candidate matrix, and either retain it for integrated W3/W5 confirmation or close W3 as measured-no-win."
