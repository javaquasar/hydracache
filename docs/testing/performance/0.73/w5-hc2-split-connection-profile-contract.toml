schema_version = 1
release = "0.73"
contract_id = "w5-hc2-split-connection-profile-073-v1"
state = "preregistered-awaiting-local-split-process-matrix"
triggering_evidence = "docs/testing/performance/0.73/w5-hc2-connection-profile-ff657fc5.toml"
source_parent = "241e1c8a57218614490515089239f2d5659ea5e1"
profile_tool = "tools/hc2-connection-profile-073"
profile_mode = "split"
server_scope = "dedicated-local-server-process"
client_scope = "dedicated-local-client-controller-process"
cardinalities = [1, 10, 100, 1000]
repeats_per_cardinality = 3
independent_process_pairs_required = true
metrics = ["client_gross_allocated_bytes", "server_gross_allocated_bytes", "client_working_set_bytes", "server_working_set_bytes", "client_pagefile_bytes", "server_pagefile_bytes"]
server_allocation_window = "After listener/TLS construction and an explicit controller handshake, through exact active-connection cardinality. The fixed counting-ready file write is included and must not be divided into a one-connection claim."
client_allocation_window = "Client vector creation through successful connection of the frozen cardinality; close is outside the allocation window."
production_counter_addition = false
product_mutation_allowed = false
candidate_data_allowed = false
dedicated_host_run_allowed = false
promotable = false
universal_per_connection_claim_allowed = false
release_numerical_claim_allowed = false
falsifier = "Reject any pair with child-process failure or stderr, missing snapshots, active/accepted/closed mismatch, nonzero client retained resources, or incomplete post-close reconciliation. Preserve negative and nonlinear deltas."
interpretation_limit = "Process separation distinguishes local client from server-side runtime ownership, but the server process still combines HydraCache, tonic/HTTP2, TLS, Tokio, system allocator, and fixed orchestration. Only multi-cardinality marginal shape may guide the next owner probe."
next_decision = "Use the split medians to choose a narrower server or client owner probe. Do not implement buffer sizing, byte admission, or idle trimming unless one endpoint and owner class is materially attributed."
